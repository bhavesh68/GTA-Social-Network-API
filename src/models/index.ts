import User from "./User.js"; // Import the User model
import { Thought, Reaction } from "./Thought.js"; // Import the Thought and Reaction models

// Export the User, Thought, and Reaction models
export { User, Thought, Reaction };
